export class Transport{
    id!: number;
    nome: string = "";
    custoPorMetroCubico!: number;

    // public Transport(nome:string, custoPorMetroCubico:number){
    //     this.nome = nome;
    //     this.custoPorMetroCubico = custoPorMetroCubico;
    // }
}